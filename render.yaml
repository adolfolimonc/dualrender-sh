# Render deployment configuration
services:
  - type: web
    name: dualrender
    env: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: NODE_ENV
        value: production
    plan: free
